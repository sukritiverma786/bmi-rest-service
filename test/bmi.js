var assert = require('assert');
describe('Array', function () {
    describe('#indexOf()', function () {
        it('BMI shold always positive number', function () {
            assert.equal([1, 2, 3].indexOf(3), 2);
        })
    })
})